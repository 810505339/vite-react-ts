import React, {FC} from 'react'
import {NavBar} from "@/components";
import GlobalStyles from "@/GlobalStyles";

const App: FC = () => {

    return (<>
        <GlobalStyles/>
        <NavBar/>
    </>)
}

export default App
